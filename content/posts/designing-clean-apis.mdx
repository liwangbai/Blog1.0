---
title: "Designing Clean REST APIs"
description: "Principles and practices for designing REST APIs that are intuitive, consistent, and maintainable."
date: "2026-02-20"
tags: ["api", "backend", "rest", "design"]
featured: false
---

A well-designed API is a joy to use. Here are the principles I follow when designing REST APIs.

## Resource Naming

Use plural nouns for collections:

```
GET    /api/posts          # List posts
GET    /api/posts/:id      # Get a post
POST   /api/posts          # Create a post
PUT    /api/posts/:id      # Update a post
DELETE /api/posts/:id      # Delete a post
```

## Consistent Responses

Every endpoint returns a consistent envelope:

```json
{
  "data": { ... },
  "meta": {
    "page": 1,
    "totalPages": 10
  },
  "error": null
}
```

## Status Codes

Use HTTP status codes correctly:

| Code | Meaning |
|------|---------|
| 200 | Success |
| 201 | Created |
| 400 | Bad Request |
| 401 | Unauthorized |
| 404 | Not Found |
| 422 | Validation Error |
| 500 | Server Error |

## Error Responses

Errors should be actionable:

```json
{
  "data": null,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid request parameters",
    "details": [
      { "field": "email", "message": "Must be a valid email address" }
    ]
  }
}
```

## Versioning

Version via URL prefix or header:

```
GET /api/v1/posts
GET /api/posts  (with Accept-Version: v1 header)
```

Good API design pays dividends in developer experience and long-term maintainability.
